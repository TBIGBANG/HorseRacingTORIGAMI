# SP版オッズ取得（文字コード対策版）

この版は SP ページの文字コードを EUC-JP 優先で読みます。

## Render 設定

Build Command:
```bash
pip install -r requirements.txt
```

Start Command:
```bash
streamlit run app.py --server.port=$PORT --server.address=0.0.0.0
```
